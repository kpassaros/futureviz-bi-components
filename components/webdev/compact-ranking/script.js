(()=>{const esc=v=>String(v??'').replace(/[&<>"']/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[m]));const num=(v,d=2)=>new Intl.NumberFormat('pt-BR',{maximumFractionDigits:d}).format(Number(v)||0);const money=v=>new Intl.NumberFormat('pt-BR',{style:'currency',currency:'BRL',maximumFractionDigits:2}).format(Number(v)||0);const pct=v=>new Intl.NumberFormat('pt-BR',{style:'percent',maximumFractionDigits:2}).format(Number(v)||0);const rootOf=r=>typeof r==='string'?document.querySelector(r):r;function render(root,data={}){const el=rootOf(root);if(!el)return;const d={title:'Ranking compacto',limit:5,items:[{name:'Norte',value:482000},{name:'Sul',value:391000},{name:'Sudeste',value:326000}],...data};const items=d.items.slice(0,d.limit),max=Math.max(...items.map(x=>x.value),1);const rows=items.map((x,i)=>`<div class="fv-row"><span>${i+1}. ${esc(x.name)}</span><b>${money(x.value)}</b><div class="fv-mini-track"><i style="width:${x.value/max*100}%"></i></div></div>`).join('');el.innerHTML=`<article class="fv-card"><header class="fv-head"><div><span class="fv-kicker">Vendas</span><h2>${esc(d.title)}</h2></div><span class="fv-badge">Top ${d.limit}</span></header><div class="fv-list">${rows}</div></article>`;}window.FutureVizCompactRanking={render};render('#futureviz-component');})();
