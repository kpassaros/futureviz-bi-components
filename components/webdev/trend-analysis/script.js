(()=>{const esc=v=>String(v??'').replace(/[&<>"']/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[m]));const num=(v,d=2)=>new Intl.NumberFormat('pt-BR',{maximumFractionDigits:d}).format(Number(v)||0);const money=v=>new Intl.NumberFormat('pt-BR',{style:'currency',currency:'BRL',maximumFractionDigits:2}).format(Number(v)||0);const pct=v=>new Intl.NumberFormat('pt-BR',{style:'percent',maximumFractionDigits:2}).format(Number(v)||0);const rootOf=r=>typeof r==='string'?document.querySelector(r):r;function render(root,data={}){const el=rootOf(root);if(!el)return;const d={title:'Análise de tendências',value:926000,week:.032,month:.084,year:.217,...data};const tile=(l,v)=>`<div class="fv-tile"><span class="fv-label">${l}</span><b class="${v>=0?'fv-positive':'fv-negative'}">${v>=0?'↑':'↓'} ${pct(Math.abs(v))}</b></div>`;el.innerHTML=`<article class="fv-card"><header class="fv-head"><div><span class="fv-kicker">Vendas</span><h2>${esc(d.title)}</h2></div></header><strong class="fv-value">${money(d.value)}</strong><div class="fv-grid">${tile('Semana',d.week)}${tile('Mês',d.month)}${tile('Ano',d.year)}</div></article>`;}window.FutureVizTrendAnalysis={render};render('#futureviz-component');})();
