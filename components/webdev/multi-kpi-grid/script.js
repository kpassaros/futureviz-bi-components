(()=>{const esc=v=>String(v??'').replace(/[&<>"']/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[m]));const num=(v,d=2)=>new Intl.NumberFormat('pt-BR',{maximumFractionDigits:d}).format(Number(v)||0);const money=v=>new Intl.NumberFormat('pt-BR',{style:'currency',currency:'BRL',maximumFractionDigits:2}).format(Number(v)||0);const pct=v=>new Intl.NumberFormat('pt-BR',{style:'percent',maximumFractionDigits:2}).format(Number(v)||0);const rootOf=r=>typeof r==='string'?document.querySelector(r):r;function render(root,data={}){const el=rootOf(root);if(!el)return;const d={title:'Visão executiva',items:[{label:'Receita',value:'R$ 428,60 mil'},{label:'Clientes',value:'1.248'},{label:'Conversão',value:'4,80%'},{label:'NPS',value:'72'}],...data};const tiles=d.items.map(x=>`<div class="fv-tile"><span class="fv-label">${esc(x.label)}</span><b>${esc(x.value)}</b></div>`).join('');el.innerHTML=`<article class="fv-card"><header class="fv-head"><div><span class="fv-kicker">Vendas</span><h2>${esc(d.title)}</h2></div><span class="fv-badge">${d.items.length} KPIs</span></header><div class="fv-grid">${tiles}</div></article>`;}window.FutureVizMultiKpiGrid={render};render('#futureviz-component');})();
