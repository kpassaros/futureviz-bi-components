(()=>{const esc=v=>String(v??'').replace(/[&<>"']/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[m]));const num=(v,d=2)=>new Intl.NumberFormat('pt-BR',{maximumFractionDigits:d}).format(Number(v)||0);const money=v=>new Intl.NumberFormat('pt-BR',{style:'currency',currency:'BRL',maximumFractionDigits:2}).format(Number(v)||0);const pct=v=>new Intl.NumberFormat('pt-BR',{style:'percent',maximumFractionDigits:2}).format(Number(v)||0);const rootOf=r=>typeof r==='string'?document.querySelector(r):r;function render(root,data={}){const el=rootOf(root);if(!el)return;const d={title:'Funil de conversão',steps:[{label:'Leads',value:2400},{label:'Oportunidades',value:820},{label:'Vendas',value:463}],...data};const max=Math.max(d.steps[0]?.value||1,1),last=d.steps.at(-1)?.value||0,conversion=last/max;const steps=d.steps.map(x=>`<div class="fv-step" style="width:${Math.max(42,x.value/max*100)}%"><span>${esc(x.label)}</span><b>${num(x.value,0)}</b></div>`).join('');el.innerHTML=`<article class="fv-card" data-theme="dark"><header class="fv-head"><div><span class="fv-kicker">Vendas</span><h2>${esc(d.title)}</h2></div><span class="fv-badge">${pct(conversion)}</span></header><div class="fv-funnel">${steps}</div></article>`;}window.FutureVizConversionFunnel={render};render('#futureviz-component');})();
